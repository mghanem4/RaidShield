# Safe fixtures

Every fixture is synthetic and uses neutral placeholders or the explicit `[GNCI_REDACTED_CATEGORY]` token. No hateful, threatening, identifying, scraped, or real community content is included. Fixtures are bundled and selected by name; arbitrary uploads and paths are not accepted.

