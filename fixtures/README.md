# Safe fixtures

Every fixture is synthetic and uses neutral placeholders or the explicit `[GNCI_REDACTED_CATEGORY]` token. No hateful, threatening, identifying, scraped, or real community content is included. Replay fixtures are bundled and selected by name; arbitrary replay paths are not accepted. Separately, Test Lab accepts strictly validated offline JSON datasets described in `docs/OFFLINE_DATA.md`.
